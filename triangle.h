float area_triangle(int b,int h)
{
	float area;
	area = (float)1/2 * b * h;
	return area;
}
int perimeter_triangle(int l,int b,int h)
{
	int perimeter;
	perimeter = l + b + h;
	return perimeter;
}
